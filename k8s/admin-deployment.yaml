apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-deployment
  labels:
    app: admin-service
spec:
  replicas: 1 # Admin service might only need 1 replica
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
      - name: admin-service
        image: your-docker-registry/admin-service:latest # <-- TODO: Replace with your actual image path
        ports:
        - containerPort: 3000 # Port exposed in the Dockerfile (assuming 3000)
        env:
        # Add any necessary environment variables here
        # Example:
        # - name: DATABASE_URL
        #   value: "your-database-url"
        # - name: KAFKA_BROKERS
        #   value: "kafka-service:9092"
        # - name: NODE_ENV
        #   value: "production"
        resources:
          limits:
            memory: "256Mi"
            cpu: "500m"
          requests:
            memory: "128Mi"
            cpu: "250m" 